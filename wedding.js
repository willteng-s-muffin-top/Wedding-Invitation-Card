// gsap.from("h1.main-title", { duration: 0.75, y: -10, opacity: 0 });
// gsap.from("h2.date", { duration: 0.75, y: -10, opacity: 0 });

// var tl = gsap.timeline();

// tl.to("h1.main-title", { duration: 0.75, y: -10, opacity: 0 });
// tl.to("h2.date", { duration: 0.75, y: -10, opacity: 0 });